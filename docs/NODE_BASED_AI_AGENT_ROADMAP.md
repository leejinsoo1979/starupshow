# Node 기반 AI 에이전트 로드맵 시스템 명세서

## 개요

이 프로젝트의 목표는 전체 작업을 노드(Node) 기반의 실행 가능한 로드맵으로 구성하고, 각 노드마다 AI 에이전트를 배치함으로써 자동 실행·분석·보고가 가능한 업무 공장 형태의 시스템(일명 **Factory OS**)을 구축하는 것이다.

이러한 시스템은 일련의 AI 작업들을 하나의 운영체제처럼 관리하여 워크플로우를 효율적으로 실행하도록 설계된다. 다수의 AI 에이전트를 유기적으로 오케스트레이션하여 자율적 협업이 가능하게 함으로써 복잡한 프로젝트도 자동으로 처리되도록 한다.

> 최근에는 이러한 에이전트 오케스트레이션을 "AI 워크플로우의 쿠버네티스(Kubernetes)"에 비유하기도 하는데, 이는 각종 에이전트를 체계적으로 관리하여 대규모 작업도 안정적으로 수행하기 위한 개념이다.

---

## 로드맵 구조

전체 로드맵은 **Phase → Epic → Node**의 3계층 단계적 구조로 구성된다.

```
┌─────────────────────────────────────────────────────────┐
│                        Phase                            │
│  (프로젝트의 주요 단계, 여러 Epic 포함)                    │
│  ┌─────────────────────────────────────────────────┐   │
│  │                     Epic                         │   │
│  │  (특정 목표 달성을 위한 관련 Node 묶음)             │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐         │   │
│  │  │  Node   │→│  Node   │→│  Node   │         │   │
│  │  │ (작업1) │  │ (작업2) │  │ (작업3) │         │   │
│  │  └─────────┘  └─────────┘  └─────────┘         │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 계층 설명

| 계층 | 설명 | 특징 |
|------|------|------|
| **Phase** | 프로젝트의 주요 단계 | 여러 Epic 포함, 최상위 단계 |
| **Epic** | 특정 목표를 달성하기 위한 일련의 관련 작업들 | 중단위 구성, 여러 Node 포함 |
| **Node** | 개별 실행 단위 (하나의 구체적인 작업) | 자율 실행 유닛, 자체 입력/출력 보유 |

### DAG 기반 의존 관계

로드맵의 노드들은 **방향성 비순환 그래프(DAG)** 형태로 의존 관계를 맺으며 연결된다:

- 어떤 Node의 출력은 다른 Node들의 입력으로 전달
- Node들 간의 선후 의존성이 그래프 구조로 표현
- 한 노드의 작업 완료가 다음 노드의 트리거 조건
- 순환 없이 각 작업이 단방향으로 흐름

---

## 에이전트 구조

에이전트 구조는 **계층형 Hierarchy**로 설계되며, 3단계로 구성된다.

```
                    ┌─────────────────────┐
                    │  Supervisor Agent   │
                    │   (전체 로드맵 총괄)  │
                    └──────────┬──────────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Phase Manager  │  │  Phase Manager  │  │  Phase Manager  │
│     Agent 1     │  │     Agent 2     │  │     Agent 3     │
└────────┬────────┘  └────────┬────────┘  └────────┬────────┘
         │                    │                    │
    ┌────┴────┐          ┌────┴────┐          ┌────┴────┐
    ▼         ▼          ▼         ▼          ▼         ▼
┌───────┐ ┌───────┐  ┌───────┐ ┌───────┐  ┌───────┐ ┌───────┐
│ Node  │ │ Node  │  │ Node  │ │ Node  │  │ Node  │ │ Node  │
│ Agent │ │ Agent │  │ Agent │ │ Agent │  │ Agent │ │ Agent │
└───────┘ └───────┘  └───────┘ └───────┘  └───────┘ └───────┘
```

### 에이전트 역할

| 에이전트 | 역할 | 책임 |
|---------|------|------|
| **Supervisor Agent** | 전체 로드맵 총괄 조율 | 전략 수립, 오류 조정, 전반적 관리 |
| **Phase Manager Agent** | Phase 내 Epic/Node 관리 | 노드 간 조정, 우선순위 변경 |
| **Node Agent** | 개별 Node 작업 실행 | 구체적 작업 수행, 결과 산출 |

### 전문화된 Node Agent 유형

```typescript
type NodeAgentType =
  | 'PlannerAgent'    // 기획 작업
  | 'UIAgent'         // 디자인 작업
  | 'DevAgent'        // 개발 작업
  | 'QAAgent'         // 테스트 작업
  | 'DataAgent'       // 데이터 분석
  | 'ContentAgent'    // 콘텐츠 생성
  | 'ResearchAgent'   // 리서치 작업
```

---

## Node 스키마 및 예시

### Node 스키마 정의

```typescript
interface Node {
  // 기본 식별 정보
  id: string;                    // 노드의 고유 ID
  goal: string;                  // 달성하려는 작업 목표

  // 입출력 정의
  input: string | InputSchema;   // 필요한 입력 데이터
  output: string | OutputSchema; // 산출되는 결과물

  // 에이전트 설정
  agent: NodeAgentType;          // 배치된 AI 에이전트 종류

  // 의존성 및 상태
  dependencies: string[];        // 선행 Node ID 목록
  status: NodeStatus;            // 현재 상태

  // 자동화 설정
  automationLevel: AutomationLevel;

  // 메타데이터
  createdAt: Date;
  updatedAt: Date;
  completedAt?: Date;
  logs?: LogEntry[];
}

type NodeStatus =
  | 'pending'      // 대기
  | 'ready'        // 준비 완료 (의존성 충족)
  | 'running'      // 실행 중
  | 'completed'    // 완료
  | 'failed'       // 실패
  | 'paused';      // 일시 중지

type AutomationLevel =
  | 'full'         // 완전 자동
  | 'assisted'     // 보조 (human-in-the-loop)
  | 'manual';      // 수동
```

### 예시: "UI 페이지 생성" 노드

```yaml
id: Create_UI_Page
goal: "디자인 시안을 바탕으로 React UI 컴포넌트 생성"
input: "완성된 Figma 디자인 파일"
output: "React JSX 코드 (디자인 구현)"
agent: DevAgent
dependencies: ["Design_Complete"]
status: "pending"
automationLevel: "full"
```

### 예시: 전체 Phase 구조

```yaml
phase: MVP_Development
  epics:
    - epic: User_Authentication
      nodes:
        - id: Design_Login_UI
          goal: "로그인 화면 UI 디자인"
          agent: UIAgent
          dependencies: []
          automationLevel: "assisted"

        - id: Implement_Auth_API
          goal: "인증 API 구현"
          agent: DevAgent
          dependencies: ["Design_Login_UI"]
          automationLevel: "full"

        - id: Test_Auth_Flow
          goal: "인증 플로우 테스트"
          agent: QAAgent
          dependencies: ["Implement_Auth_API"]
          automationLevel: "full"
```

---

## 실행 흐름

### 기본 실행 사이클

```
┌─────────────────────────────────────────────────────────────┐
│                     실행 흐름 다이어그램                       │
└─────────────────────────────────────────────────────────────┘

    ┌──────────┐
    │ Pending  │ ← 초기 상태
    └────┬─────┘
         │ 의존성 충족
         ▼
    ┌──────────┐
    │  Ready   │ ← 실행 대기
    └────┬─────┘
         │ 트리거 (auto/manual)
         ▼
    ┌──────────┐
    │ Running  │ ← 에이전트 실행 중
    └────┬─────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│Complete│ │ Failed │
└────┬───┘ └────┬───┘
     │          │
     │          ▼
     │     ┌─────────┐
     │     │  Retry  │→ (재시도 후 Running으로)
     │     └─────────┘
     │
     ▼
[다음 Node 트리거]
```

### 상세 실행 단계

#### 1단계: 의존성 만족 및 준비

```typescript
async function checkNodeReadiness(node: Node): Promise<boolean> {
  // 모든 선행 노드가 완료되었는지 확인
  const allDependenciesComplete = node.dependencies.every(
    depId => getNode(depId).status === 'completed'
  );

  if (allDependenciesComplete) {
    node.status = 'ready';

    // automationLevel에 따른 처리
    if (node.automationLevel === 'full') {
      await scheduleExecution(node);
    } else if (node.automationLevel === 'assisted') {
      await notifyForApproval(node);
    }
    // manual인 경우 사용자 지시 대기
  }

  return allDependenciesComplete;
}
```

#### 2단계: 에이전트 실행

```typescript
async function executeNode(node: Node): Promise<NodeResult> {
  node.status = 'running';

  const agent = getAgent(node.agent);
  const input = collectInputs(node);

  try {
    // Perceive → Reason → Act 사이클
    const result = await agent.execute({
      goal: node.goal,
      input: input,
      tools: agent.availableTools,
    });

    return result;
  } catch (error) {
    throw new NodeExecutionError(node.id, error);
  }
}
```

#### 3단계: 출력 생산 및 전파

```typescript
async function handleNodeCompletion(node: Node, result: NodeResult) {
  node.status = 'completed';
  node.output = result.output;
  node.completedAt = new Date();

  // 다음 노드들에게 출력 전파
  const dependentNodes = findDependentNodes(node.id);

  for (const nextNode of dependentNodes) {
    nextNode.inputs[node.id] = result.output;
    await checkNodeReadiness(nextNode);
  }
}
```

#### 4단계: 오류 처리 및 재시도

```typescript
async function handleNodeFailure(node: Node, error: Error) {
  node.status = 'failed';
  node.logs.push({ type: 'error', message: error.message });

  // 재시도 로직
  if (node.retryCount < MAX_RETRIES) {
    node.retryCount++;
    await delay(RETRY_DELAY * node.retryCount);
    await executeNode(node);
  } else {
    // Phase Manager에게 에스컬레이션
    await escalateToPhaseManager(node, error);
  }
}
```

---

## 자동화 수준 옵션

### 세 가지 자동화 모드

| 모드 | 설명 | 인간 개입 | 사용 케이스 |
|------|------|----------|------------|
| **Full** | 완전 자동 | 없음 | 검증된 반복 작업, 테스트 자동화 |
| **Assisted** | 보조 자동화 | 핵심 단계에서 승인 | 코드 리뷰, 배포 승인 |
| **Manual** | 완전 수동 | 모든 단계 | 규제 준수, 고위험 결정 |

### Full (완전 자동 모드)

```typescript
// Full 모드 예시
{
  id: "Run_Unit_Tests",
  automationLevel: "full",
  // 의존성 충족 시 즉시 자동 실행
  // 결과가 성공이면 바로 다음 노드 트리거
  // 실패 시 자동 재시도 후 에스컬레이션
}
```

**특징:**
- 인간 개입 없이 에이전트가 자율 실행
- 의존성 충족 시 즉시 실행
- 속도와 효율 극대화
- 잘못된 결과 발생 위험 존재

### Assisted (보조 모드)

```typescript
// Assisted 모드 예시
{
  id: "Deploy_to_Production",
  automationLevel: "assisted",
  approvalRequired: true,
  approvalStages: ["before_deploy", "after_deploy"],
  // 에이전트가 준비 완료 후 인간 승인 대기
  // 승인 후 실행, 완료 후 결과 검토
}
```

**특징:**
- Human-in-the-loop 방식
- 핵심 단계에서 사용자 확인/승인
- AI 효율성 + 인간 판단 결합
- 품질 통제 유지

### Manual (수동 모드)

```typescript
// Manual 모드 예시
{
  id: "Legal_Document_Review",
  automationLevel: "manual",
  // 모든 단계가 사람에 의해 트리거
  // 에이전트는 초안/제안만 제공
  // 최종 결정은 항상 인간이 수행
}
```

**특징:**
- 전통적 수동 작업과 유사
- 에이전트는 제안/초안 제공
- 모든 결정을 인간이 수행
- 고신뢰성 필요 작업에 적합

---

## 시각화 및 OS형 UI

### 대시보드 구성 요소

```
┌─────────────────────────────────────────────────────────────────┐
│                    Factory OS Dashboard                          │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                   Node Graph View (DAG)                      │ │
│ │                                                              │ │
│ │   [Node A] ──→ [Node B] ──→ [Node D]                        │ │
│ │       │            │            │                            │ │
│ │       └──→ [Node C] ──────────┘                             │ │
│ │                                                              │ │
│ │   🟢 Completed  🟡 Running  ⚪ Pending  🔴 Failed           │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│ ┌──────────────────────┐  ┌──────────────────────────────────┐ │
│ │   Phase Progress     │  │       Node Details Panel          │ │
│ │                      │  │                                    │ │
│ │ Phase 1: ████████ 80%│  │ ID: Create_UI_Page                │ │
│ │ Phase 2: ████░░░░ 45%│  │ Status: Running                   │ │
│ │ Phase 3: ░░░░░░░░  0%│  │ Agent: DevAgent                   │ │
│ │                      │  │ Progress: 67%                     │ │
│ │                      │  │ Logs: [View]                      │ │
│ └──────────────────────┘  └──────────────────────────────────┘ │
│                                                                  │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                    Activity Log / Agent Messages             │ │
│ │ 14:32:01 [DevAgent] Starting code generation...              │ │
│ │ 14:32:15 [DevAgent] Generated 3 components                   │ │
│ │ 14:32:20 [QAAgent] Running lint checks...                    │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### UI 기능 요구사항

#### 그래프 시각화
- DAG 형태의 노드 그래프 표시
- 노드 상태별 색상/아이콘 구분
- 엣지(화살표)로 의존 관계 표현
- Drag & Zoom 인터랙션 지원
- 실시간 상태 업데이트

#### 노드 상세 정보
- 클릭 시 세부 정보 패널 표시
- 목표, 에이전트, 입출력 데이터
- 현재 상태, 시작/종료 시간
- 실행 로그 및 오류 정보

#### 제어 기능
- 노드 수동 실행/중지/재시도
- Phase 일시 정지/재개
- 의존 관계 수정 (편집 모드)
- 우선순위 변경

#### 모니터링
- Phase/Epic 단위 진행률
- 에이전트 리소스 사용량
- 실패율 및 성능 지표
- 실시간 알림

---

## 사용 기술 및 구현 방안

### 기술 스택 개요

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend Layer                          │
│  React.js + TypeScript + D3.js/Three.js + WebSocket         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      API Layer                               │
│           Next.js API Routes / Express.js                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Agent Orchestration Layer                  │
│            Python + LangChain/LangGraph                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Data Layer                              │
│     PostgreSQL + Redis (Queue) + Elasticsearch (Logs)       │
└─────────────────────────────────────────────────────────────┘
```

### 상세 기술 스택

#### 언어 및 런타임
| 영역 | 기술 | 용도 |
|------|------|------|
| Backend (Agent) | Python 3.11+ | AI/ML 라이브러리, 에이전트 구현 |
| Backend (API) | TypeScript/Node.js | 웹 API, 실시간 통신 |
| Frontend | TypeScript/React | 대시보드 UI |

#### 에이전트 프레임워크
| 기술 | 용도 |
|------|------|
| **LangChain** | LLM 에이전트 체인 구축, 도구 연계 |
| **LangGraph** | DAG 형태 워크플로우 구성 |
| **Custom Orchestrator** | 특화 요구사항 처리 |

#### 프론트엔드 시각화
| 기술 | 용도 |
|------|------|
| **React.js** | 컴포넌트 기반 UI |
| **D3.js** | 2D 노드 그래프 시각화 |
| **Three.js** | 3D 시각화 (선택적) |
| **WebSocket** | 실시간 상태 업데이트 |

#### 데이터 및 인프라
| 기술 | 용도 |
|------|------|
| **PostgreSQL** | 로드맵, 노드 메타데이터 저장 |
| **MongoDB** | 에이전트 로그 (비정형) |
| **Redis/RabbitMQ** | 메시지 큐, 이벤트 처리 |
| **Elasticsearch** | 로그 수집 및 검색 |
| **Grafana** | 메트릭 시각화 |

### 구현 아키텍처

```typescript
// Node 실행 엔진 예시 구조
interface NodeExecutionEngine {
  // 노드 스케줄링
  scheduleNode(nodeId: string): Promise<void>;

  // 에이전트 실행
  executeWithAgent(node: Node): Promise<NodeResult>;

  // 상태 관리
  updateNodeStatus(nodeId: string, status: NodeStatus): void;

  // 이벤트 발행
  emitNodeEvent(event: NodeEvent): void;

  // 에러 핸들링
  handleFailure(nodeId: string, error: Error): Promise<void>;
}

// 에이전트 인터페이스
interface Agent {
  type: NodeAgentType;
  tools: Tool[];

  execute(task: AgentTask): Promise<AgentResult>;
  perceive(input: any): Promise<Perception>;
  reason(perception: Perception): Promise<Plan>;
  act(plan: Plan): Promise<ActionResult>;
}
```

---

## 확장 고려사항

### 스케일링 전략
- 수평적 확장: 여러 Phase를 병렬 처리
- 에이전트 풀: 동일 유형 에이전트 다중 인스턴스
- 분산 실행: 쿠버네티스 기반 컨테이너 오케스트레이션

### 보안 고려사항
- 에이전트 권한 제한 (최소 권한 원칙)
- 민감 데이터 암호화
- 감사 로그 유지
- 접근 제어 (RBAC)

### 모니터링 및 알림
- 노드 실패 시 즉각 알림
- 성능 임계치 모니터링
- 이상 탐지 자동화
- SLA 추적

---

## 결론

본 Node 기반 AI 에이전트 로드맵 시스템은 복잡한 프로젝트를 자동화된 워크플로우로 분해하고, 각 작업에 전문화된 AI 에이전트를 배치하여 자율적으로 실행하는 **Factory OS** 개념을 구현한다.

핵심 특징:
1. **Phase → Epic → Node** 3계층 구조로 체계적 작업 분해
2. **계층적 에이전트 구조**로 효율적인 오케스트레이션
3. **DAG 기반 의존성 관리**로 복잡한 워크플로우 지원
4. **유연한 자동화 수준**으로 인간-AI 협업 최적화
5. **실시간 시각화 UI**로 투명한 프로젝트 관리

이 시스템을 통해 팀은 복잡한 AI 업무 파이프라인을 하나의 화면에서 관리하고, 에이전트들이 공장 생산라인처럼 유기적으로 협업하여 결과물을 만들어내는 과정을 효율적으로 통제할 수 있다.

# 02. 아키텍처

## 🏛️ 전체 시스템 구조

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           NeuraMap Coding Agent                             │
│                         (GlowUS Platform Integration)                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │                        Frontend Layer (React)                      │     │
│  ├────────────────────────────────────────────────────────────────────┤     │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │     │
│  │  │  Chat UI     │  │ Plan Viewer  │  │ Approval     │             │     │
│  │  │  (Input)     │  │ (plan.md)    │  │ Gate UI      │             │     │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘             │     │
│  │         │                 │                 │                      │     │
│  │  ┌──────┴─────────────────┴─────────────────┴───────┐             │     │
│  │  │           Agent Chat Panel (AgentChatPanel)      │             │     │
│  │  └──────────────────────────┬───────────────────────┘             │     │
│  │                             │                                      │     │
│  │  ┌──────────────────────────┴───────────────────────┐             │     │
│  │  │         Execution Monitor (ExecutionPanel)       │             │     │
│  │  │  - Streaming updates                             │             │     │
│  │  │  - Real-time node execution tracking             │             │     │
│  │  └──────────────────────────┬───────────────────────┘             │     │
│  └────────────────────────────┬┴────────────────────────────────────┘     │
│                               │                                            │
│                               │ WebSocket / SSE                            │
│                               ▼                                            │
│  ┌─────────────────────────────────────────────────────────────────┐      │
│  │                     Orchestration Layer                          │      │
│  ├─────────────────────────────────────────────────────────────────┤      │
│  │                                                                  │      │
│  │  ┌────────────────────────────────────────────────────────────┐ │      │
│  │  │         Agentic Loop Engine (LangGraph StateGraph)         │ │      │
│  │  ├────────────────────────────────────────────────────────────┤ │      │
│  │  │                                                             │ │      │
│  │  │  ┌──────────┐      ┌──────────┐      ┌──────────┐         │ │      │
│  │  │  │  PLAN    │  →   │  MODIFY  │  →   │  VERIFY  │         │ │      │
│  │  │  │          │      │          │      │          │         │ │      │
│  │  │  │ - Task   │      │ - Apply  │      │ - Build  │         │ │      │
│  │  │  │   Break  │      │   Patch  │      │ - Test   │         │ │      │
│  │  │  │ - Context│      │ - Multi  │      │ - Lint   │         │ │      │
│  │  │  │   Gather │      │   File   │      │          │         │ │      │
│  │  │  └────┬─────┘      └────┬─────┘      └────┬─────┘         │ │      │
│  │  │       │                 │                 │                │ │      │
│  │  │       └─────────────────┴─────────────────┘                │ │      │
│  │  │                         │                                  │ │      │
│  │  │                         ▼                                  │ │      │
│  │  │                  ┌──────────┐                              │ │      │
│  │  │                  │  COMMIT  │                              │ │      │
│  │  │                  │          │                              │ │      │
│  │  │                  │ - Git    │                              │ │      │
│  │  │                  │   Commit │                              │ │      │
│  │  │                  │ - Update │                              │ │      │
│  │  │                  │   Memory │                              │ │      │
│  │  │                  └──────────┘                              │ │      │
│  │  │                                                             │ │      │
│  │  └─────────────────────────────────────────────────────────┬─┘ │      │
│  │                                                             │   │      │
│  │  ┌──────────────────────────────────────────────────────────┴─┐ │      │
│  │  │              State Management (AgentState)                 │ │      │
│  │  │  - messages: BaseMessage[]                                 │ │      │
│  │  │  - context: { files, symbols, diagnostics }                │ │      │
│  │  │  - plan: Task[]                                            │ │      │
│  │  │  - currentTask: Task | null                                │ │      │
│  │  │  - toolCallsCount: number                                  │ │      │
│  │  │  - error: string | null                                    │ │      │
│  │  │  - metadata: { model, timing, threadId }                   │ │      │
│  │  └────────────────────────────────────────────────────────────┘ │      │
│  └─────────────────────────────────────────────────────────────────┘      │
│                               │                                            │
│                               ▼                                            │
│  ┌─────────────────────────────────────────────────────────────────┐      │
│  │                        Tool Layer (API)                          │      │
│  ├─────────────────────────────────────────────────────────────────┤      │
│  │                                                                  │      │
│  │  ┌───────────────┬───────────────┬───────────────┬────────────┐ │      │
│  │  │ repo.search   │ repo.read     │ repo.symbols  │ repo.run   │ │      │
│  │  │ - grep        │ - File I/O    │ - LSP lookup  │ - Shell    │ │      │
│  │  │ - Glob        │ - Range read  │ - ctags       │ - Timeout  │ │      │
│  │  └───────────────┴───────────────┴───────────────┴────────────┘ │      │
│  │                                                                  │      │
│  │  ┌───────────────┬───────────────┬───────────────┬────────────┐ │      │
│  │  │ repo.patch    │ repo.diag     │ repo.git      │ repo.lsp   │ │      │
│  │  │ - apply_patch │ - Errors      │ - Commit      │ - GoToDef  │ │      │
│  │  │ - Multi-file  │ - Warnings    │ - Status      │ - FindRefs │ │      │
│  │  └───────────────┴───────────────┴───────────────┴────────────┘ │      │
│  └─────────────────────────────────────────────────────────────────┘      │
│                               │                                            │
│                               ▼                                            │
│  ┌─────────────────────────────────────────────────────────────────┐      │
│  │                    Execution & Context Layer                     │      │
│  ├─────────────────────────────────────────────────────────────────┤      │
│  │                                                                  │      │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │      │
│  │  │ Terminal Server  │  │ File System      │  │ Code Graph    │ │      │
│  │  │                  │  │ Interface        │  │ (LSP/ctags)   │ │      │
│  │  │ - WebSocket      │  │                  │  │               │ │      │
│  │  │ - node-pty       │  │ - Read/Write     │  │ - Symbols     │ │      │
│  │  │ - Multi-client   │  │ - Watch          │  │ - References  │ │      │
│  │  └──────────────────┘  └──────────────────┘  └───────────────┘ │      │
│  │                                                                  │      │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │      │
│  │  │ GCC Memory       │  │ Plan Manager     │  │ Multi-Model   │ │      │
│  │  │                  │  │                  │  │ Router        │ │      │
│  │  │ - .GCC/ folder   │  │ - plan.md        │  │               │ │      │
│  │  │ - COMMIT/BRANCH  │  │ - Tasks          │  │ - GPT-4o      │ │      │
│  │  │ - Checkpoints    │  │ - Progress       │  │ - Claude      │ │      │
│  │  └──────────────────┘  └──────────────────┘  │ - Grok        │ │      │
│  │                                               │ - Gemini      │ │      │
│  │                                               └───────────────┘ │      │
│  └─────────────────────────────────────────────────────────────────┘      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Agentic Loop 상세 설계

### 핵심 패턴: ReAct (Reason-Act-Observe)

```
User Request
    │
    ▼
┌───────────────────────────────────────────────────┐
│  1. PLAN (계획 수립)                               │
│  ────────────────────────────────────────────     │
│  🎯 Objective: 작업 분해 및 컨텍스트 수집           │
│                                                    │
│  Input:                                           │
│  - User request (natural language)                │
│  - Current codebase context                       │
│                                                    │
│  Process:                                         │
│  1. Request understanding                         │
│  2. Task decomposition (WBS)                      │
│  3. Context gathering:                            │
│     - repo.search() → Find relevant files         │
│     - repo.symbols() → Locate definitions         │
│     - repo.read() → Get detailed context          │
│  4. Risk assessment                               │
│  5. Generate plan.md                              │
│                                                    │
│  Output:                                          │
│  - Structured task list (plan.md)                 │
│  - Estimated impact                               │
│  - Required approvals                             │
│                                                    │
│  User Approval Gate: ✋ Review & Approve           │
└───────────────────────────────────────────────────┘
    │
    ▼ (Approved)
┌───────────────────────────────────────────────────┐
│  2. MODIFY (코드 수정)                             │
│  ────────────────────────────────────────────     │
│  🔧 Objective: 계획된 작업 실행                     │
│                                                    │
│  Input:                                           │
│  - Approved plan                                  │
│  - Current task from plan                         │
│                                                    │
│  Process:                                         │
│  For each task in plan:                           │
│    1. Read current file(s)                        │
│    2. Generate patch (apply_patch format)         │
│    3. Preview changes (diff)                      │
│    4. Apply patch                                 │
│    5. Update plan.md (check task)                 │
│                                                    │
│  Output:                                          │
│  - Modified files                                 │
│  - Change summary                                 │
│  - Diff preview                                   │
│                                                    │
│  User Review Gate: 👀 Inspect Changes              │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│  3. VERIFY (검증)                                  │
│  ────────────────────────────────────────────     │
│  ✅ Objective: 변경 사항 품질 보증                  │
│                                                    │
│  Input:                                           │
│  - Modified codebase                              │
│  - Test suite                                     │
│                                                    │
│  Process:                                         │
│  1. Build check:                                  │
│     - repo.run("npm run build")                   │
│     - Syntax/type errors                          │
│                                                    │
│  2. Lint check:                                   │
│     - repo.run("npm run lint")                    │
│     - Style violations                            │
│                                                    │
│  3. Test execution:                               │
│     - repo.run("npm test")                        │
│     - Unit + Integration tests                    │
│                                                    │
│  4. Diagnostics:                                  │
│     - repo.diagnostics()                          │
│     - Aggregate results                           │
│                                                    │
│  Output:                                          │
│  - Test results                                   │
│  - Error/warning list                             │
│  - Pass/Fail status                               │
│                                                    │
│  Decision Point:                                  │
│  - ✅ All pass → COMMIT                           │
│  - ❌ Failures → Retry MODIFY or Ask User         │
└───────────────────────────────────────────────────┘
    │
    ▼ (All tests pass)
┌───────────────────────────────────────────────────┐
│  4. COMMIT (변경 반영)                             │
│  ────────────────────────────────────────────     │
│  📦 Objective: 변경 사항 영구 기록                  │
│                                                    │
│  Input:                                           │
│  - Verified changes                               │
│  - Test results                                   │
│                                                    │
│  Process:                                         │
│  1. Generate commit message:                      │
│     - Summarize changes                           │
│     - Include task reference                      │
│     - Add test results                            │
│                                                    │
│  2. Git operations:                               │
│     - repo.git("add .")                           │
│     - repo.git("commit -m '...'")                 │
│                                                    │
│  3. Update GCC memory:                            │
│     - GCC.COMMIT(checkpoint)                      │
│     - Save state snapshot                         │
│                                                    │
│  4. Update plan.md:                               │
│     - Mark task as completed                      │
│     - Record execution time                       │
│     - Log any learnings                           │
│                                                    │
│  Output:                                          │
│  - Git commit SHA                                 │
│  - Updated plan.md                                │
│  - GCC checkpoint ID                              │
│                                                    │
│  Final Approval Gate: 🎉 Merge or Continue         │
└───────────────────────────────────────────────────┘
    │
    ▼
  Done ✨
  (or loop to next task)
```

---

## 📊 State Management (AgentState)

### TypeScript 타입 정의

```typescript
interface AgentState {
  // LangGraph 메시지 체인
  messages: BaseMessage[];

  // 코드베이스 컨텍스트
  context: {
    files: FileContext[];          // 열려 있는 파일 정보
    symbols: SymbolIndex[];        // 심볼 인덱스
    diagnostics: Diagnostic[];     // 에러/경고
    dependencies: DependencyGraph; // 의존성 그래프
  };

  // 현재 플랜
  plan: {
    tasks: Task[];                // 작업 목록
    currentTaskIndex: number;     // 현재 진행 중인 작업
    approvalStatus: 'pending' | 'approved' | 'rejected';
  };

  // 실행 상태
  execution: {
    stage: 'plan' | 'modify' | 'verify' | 'commit';
    toolCallsCount: number;
    lastToolResult: string | null;
    error: string | null;
  };

  // 메타데이터
  metadata: {
    model: string;               // 사용 중인 LLM
    startTime: number;
    threadId: string;
    userId: string;
  };

  // GCC 메모리
  memory: {
    checkpoints: Checkpoint[];
    currentBranch: string;
    workingDirectory: string;
  };
}

interface Task {
  id: string;
  description: string;
  status: 'pending' | 'in_progress' | 'completed' | 'failed';
  files: string[];               // 영향 받는 파일
  estimatedRisk: 'low' | 'medium' | 'high';
  requiredApproval: boolean;
  startTime?: number;
  endTime?: number;
  error?: string;
}

interface FileContext {
  path: string;
  content: string;
  language: string;
  lastModified: number;
  symbols: SymbolInfo[];
}

interface SymbolInfo {
  name: string;
  kind: 'function' | 'class' | 'variable' | 'interface' | 'type';
  location: {
    file: string;
    line: number;
    column: number;
  };
  references: SymbolReference[];
}

interface Diagnostic {
  severity: 'error' | 'warning' | 'info';
  message: string;
  file: string;
  line: number;
  source: 'build' | 'lint' | 'test';
}
```

---

## 🔌 Tool Layer Architecture

### Tool Registry 구조

```typescript
interface ToolDefinition {
  name: string;
  description: string;
  parameters: JSONSchema;
  handler: ToolHandler;
  permissions: Permission[];
  timeout?: number;
}

type ToolHandler = (params: any, context: AgentState) => Promise<ToolResult>;

interface ToolResult {
  success: boolean;
  data?: any;
  error?: string;
  metadata?: {
    executionTime: number;
    resourceUsage?: ResourceMetrics;
  };
}

// 도구 목록
const CODING_TOOLS: ToolDefinition[] = [
  {
    name: 'repo.search',
    description: '코드베이스에서 패턴 검색',
    parameters: { query: 'string', path?: 'string', type?: 'string' },
    handler: searchHandler,
    permissions: ['read'],
  },
  {
    name: 'repo.read',
    description: '파일 내용 읽기',
    parameters: { file: 'string', startLine?: 'number', endLine?: 'number' },
    handler: readHandler,
    permissions: ['read'],
  },
  {
    name: 'repo.symbols',
    description: '심볼 정의 조회',
    parameters: { name: 'string', kind?: 'string' },
    handler: symbolsHandler,
    permissions: ['read'],
  },
  {
    name: 'repo.patch',
    description: '구조화된 패치 적용',
    parameters: { operations: 'PatchOperation[]' },
    handler: patchHandler,
    permissions: ['write'],
    timeout: 30000,
  },
  {
    name: 'repo.run',
    description: '쉘 명령 실행',
    parameters: { command: 'string', cwd?: 'string', timeout?: 'number' },
    handler: runHandler,
    permissions: ['execute'],
    timeout: 60000,
  },
  {
    name: 'repo.diagnostics',
    description: '빌드/테스트 결과 조회',
    parameters: {},
    handler: diagnosticsHandler,
    permissions: ['read'],
  },
  {
    name: 'repo.git',
    description: 'Git 명령 실행',
    parameters: { command: 'string', args?: 'string[]' },
    handler: gitHandler,
    permissions: ['write', 'execute'],
  },
  {
    name: 'repo.lsp',
    description: 'LSP 서버 쿼리',
    parameters: { method: 'string', params: 'any' },
    handler: lspHandler,
    permissions: ['read'],
  },
];
```

---

## 🌐 Data Flow

### 1. User Request → Plan Generation

```
User: "로그인 기능에 단위테스트 추가해줘"
    │
    ▼
ChatInput (Frontend)
    │ WebSocket
    ▼
LangGraph Executor (Backend)
    │
    ├─→ repo.search("auth", "login")
    │   └─→ ["src/auth/login.ts", "src/auth/index.ts"]
    │
    ├─→ repo.read("src/auth/login.ts")
    │   └─→ [Function definitions, current logic]
    │
    ├─→ repo.symbols("validateLogin")
    │   └─→ [Location, references]
    │
    └─→ Generate Plan
        └─→ plan.md:
            - [ ] Task 1: auth.ts에 입력 검증 추가
            - [ ] Task 2: auth.test.ts 생성
            - [ ] Task 3: 테스트 케이스 작성
            - [ ] Task 4: 실행 및 검증
    │
    ▼ (SSE Stream)
PlanViewer (Frontend)
    │
    └─→ User reviews and clicks "Approve"
```

### 2. Plan Execution → Code Modification

```
User clicks "Approve"
    │
    ▼
LangGraph: MODIFY stage
    │
    ├─→ Task 1: repo.patch([
    │       {
    │         op: 'modify',
    │         path: 'src/auth/login.ts',
    │         changes: [{
    │           oldText: 'function validateLogin(email, password) {',
    │           newText: 'function validateLogin(email: string, password: string) {\n  if (!email || !password) throw new Error("Missing credentials");'
    │         }]
    │       }
    │   ])
    │   └─→ File modified ✅
    │
    ├─→ Task 2: repo.patch([
    │       {
    │         op: 'create',
    │         path: 'src/auth/login.test.ts',
    │         content: 'import { validateLogin } from "./login";\n\ndescribe("validateLogin", () => { ... });'
    │       }
    │   ])
    │   └─→ File created ✅
    │
    └─→ Update plan.md:
        - [x] Task 1: auth.ts에 입력 검증 추가
        - [x] Task 2: auth.test.ts 생성
        - [ ] Task 3: 테스트 케이스 작성 (in progress)
```

### 3. Verification → Commit

```
LangGraph: VERIFY stage
    │
    ├─→ repo.run("npm run build")
    │   └─→ ✅ Build successful
    │
    ├─→ repo.run("npm run lint")
    │   └─→ ✅ No errors
    │
    ├─→ repo.run("npm test")
    │   └─→ ✅ All tests pass (5/5)
    │
    └─→ repo.diagnostics()
        └─→ { errors: 0, warnings: 0 }
    │
    ▼ (All pass)
LangGraph: COMMIT stage
    │
    ├─→ repo.git("add", ["src/auth/login.ts", "src/auth/login.test.ts"])
    │
    ├─→ repo.git("commit", ["-m", "feat: Add input validation and unit tests for login"])
    │   └─→ Commit SHA: abc123
    │
    ├─→ GCC.COMMIT({
    │     checkpoint: "Task completed: login tests",
    │     files: [...],
    │     timestamp: Date.now()
    │   })
    │
    └─→ Update plan.md:
        - [x] Task 1: auth.ts에 입력 검증 추가
        - [x] Task 2: auth.test.ts 생성
        - [x] Task 3: 테스트 케이스 작성
        - [x] Task 4: 실행 및 검증 ✅
    │
    ▼
ExecutionPanel (Frontend)
    │
    └─→ Show success message + commit SHA
```

---

## 🧩 Component Dependencies

```
┌─────────────────────────────────────────┐
│         Frontend Components             │
├─────────────────────────────────────────┤
│  ChatInput                              │
│    └─→ uses: WebSocket, AgentState      │
│                                         │
│  PlanViewer                             │
│    └─→ uses: plan.md, ApprovalGate      │
│                                         │
│  ExecutionPanel                         │
│    └─→ uses: SSE, AgentState, Logs      │
│                                         │
│  ApprovalGate                           │
│    └─→ uses: Task, Risk Assessment      │
└─────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│      Backend Components                 │
├─────────────────────────────────────────┤
│  LangGraph Executor                     │
│    └─→ uses: StateGraph, Tools, LLM     │
│                                         │
│  Tool Handlers                          │
│    ├─→ searchHandler                    │
│    ├─→ readHandler                      │
│    ├─→ patchHandler                     │
│    ├─→ runHandler                       │
│    └─→ gitHandler                       │
│                                         │
│  Plan Manager                           │
│    └─→ uses: plan.md parser, Tasks      │
│                                         │
│  GCC Memory                             │
│    └─→ uses: .GCC/ folder, Git          │
└─────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│      Infrastructure Layer               │
├─────────────────────────────────────────┤
│  Terminal Server (WebSocket)            │
│  File System Interface                  │
│  LSP Client                             │
│  Multi-Model Router                     │
└─────────────────────────────────────────┘
```

---

## 📚 다음 문서

➡️ **[03-API-SPECIFICATION.md](./03-API-SPECIFICATION.md)** - 전체 API 명세와 스키마 정의

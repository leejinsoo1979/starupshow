import { NextResponse } from 'next/server'
import { GoogleGenerativeAI } from '@google/generative-ai'

// Lazy initialization
let genAI: GoogleGenerativeAI | null = null

function getGeminiClient(): GoogleGenerativeAI {
  if (!genAI) {
    const apiKey = process.env.GOOGLE_API_KEY
    if (!apiKey) {
      throw new Error('GOOGLE_API_KEY is not configured')
    }
    genAI = new GoogleGenerativeAI(apiKey)
  }
  return genAI
}

interface Source {
  id: string
  type: 'pdf' | 'web' | 'youtube' | 'text' | 'image'
  title: string
  content?: string
  summary?: string
}

interface ImageSource {
  title: string
  imageDataUrl: string // base64 data URL
}

type GenerationType = 'briefing' | 'faq' | 'timeline' | 'study-guide' | 'deep-dive' | 'slides' | 'video-overview' | 'briefing-doc' | 'audio-overview' | 'mindmap' | 'report' | 'flashcard' | 'quiz' | 'infographic' | 'data-table'

const GENERATION_PROMPTS: Record<GenerationType, { systemPrompt: string; outputFormat: string }> = {
  'briefing': {
    systemPrompt: `당신은 전문 비즈니스 분석가입니다. 제공된 자료를 기반으로 전문적인 브리핑 문서를 작성합니다.`,
    outputFormat: `# 브리핑 문서

## 📋 요약 (Executive Summary)
[2-3문장으로 핵심 내용 요약]

## 🔑 핵심 포인트
1. [주요 발견사항 1]
2. [주요 발견사항 2]
3. [주요 발견사항 3]

## 📊 상세 분석
[자료의 주요 내용을 구조화하여 설명]

## 💡 시사점 및 권장사항
- [권장사항 1]
- [권장사항 2]
- [권장사항 3]

## 📝 추가 참고사항
[관련 컨텍스트나 배경 정보]`
  },
  'faq': {
    systemPrompt: `당신은 Q&A 전문가입니다. 제공된 자료를 기반으로 예상되는 질문과 명확한 답변을 생성합니다.`,
    outputFormat: `# 자주 묻는 질문 (FAQ)

**Q1. [질문]**
A: [답변]

**Q2. [질문]**
A: [답변]

**Q3. [질문]**
A: [답변]

**Q4. [질문]**
A: [답변]

**Q5. [질문]**
A: [답변]

**Q6. [질문]**
A: [답변]

**Q7. [질문]**
A: [답변]

**Q8. [질문]**
A: [답변]

**Q9. [질문]**
A: [답변]

**Q10. [질문]**
A: [답변]`
  },
  'timeline': {
    systemPrompt: `당신은 타임라인 분석 전문가입니다. 제공된 자료에서 시간순 이벤트와 단계를 추출하여 명확한 타임라인을 작성합니다.`,
    outputFormat: `# 타임라인

## 🕐 주요 이벤트/단계

### [시점/단계 1]
- **이벤트**: [설명]
- **영향/결과**: [설명]

### [시점/단계 2]
- **이벤트**: [설명]
- **영향/결과**: [설명]

### [시점/단계 3]
- **이벤트**: [설명]
- **영향/결과**: [설명]

## 📈 전체 흐름 요약
[전체적인 흐름과 패턴 설명]

## 🔮 향후 예상
[향후 진행 방향이나 예상 사항]`
  },
  'study-guide': {
    systemPrompt: `당신은 교육 콘텐츠 전문가입니다. 제공된 자료를 기반으로 효과적인 학습 가이드를 작성합니다.`,
    outputFormat: `# 학습 가이드

## 📚 학습 목표
이 가이드를 통해 배울 수 있는 내용:
1. [목표 1]
2. [목표 2]
3. [목표 3]

## 🔤 핵심 개념 및 용어
| 용어 | 설명 |
|------|------|
| [용어 1] | [설명] |
| [용어 2] | [설명] |
| [용어 3] | [설명] |

## 📖 주요 내용 정리

### 섹션 1: [제목]
[핵심 내용 설명]

### 섹션 2: [제목]
[핵심 내용 설명]

### 섹션 3: [제목]
[핵심 내용 설명]

## ✏️ 연습 문제
1. [문제 1]
2. [문제 2]
3. [문제 3]

## 💡 핵심 정리
- [핵심 포인트 1]
- [핵심 포인트 2]
- [핵심 포인트 3]

## 📎 추가 학습 자료
[관련 주제나 심화 학습을 위한 제안]`
  },
  'deep-dive': {
    systemPrompt: `당신은 심층 분석 전문가입니다. 제공된 자료를 깊이 분석하여 숨겨진 통찰, 패턴, 연결고리를 발견합니다.`,
    outputFormat: `# 심층 분석 (Deep Dive)

## 🔬 분석 개요
[분석 대상과 접근 방법 설명]

## 🧩 핵심 발견사항

### 발견 1: [제목]
- **관찰**: [무엇을 발견했는지]
- **의미**: [왜 중요한지]
- **영향**: [어떤 영향이 있는지]

### 발견 2: [제목]
- **관찰**: [무엇을 발견했는지]
- **의미**: [왜 중요한지]
- **영향**: [어떤 영향이 있는지]

## 🔗 패턴 및 연결고리
[데이터 간의 관계, 반복되는 패턴, 숨겨진 연결]

## 💎 숨겨진 통찰
1. [통찰 1]
2. [통찰 2]
3. [통찰 3]

## ⚡ 강점과 약점 분석
**강점:**
- [강점 1]
- [강점 2]

**약점/개선점:**
- [약점 1]
- [약점 2]

## 🎯 전략적 제안
[분석 결과를 기반으로 한 실행 가능한 제안]

## 📌 결론
[핵심 메시지와 다음 단계]`
  },
  'slides': {
    systemPrompt: `당신은 프레젠테이션 전문가입니다. 제공된 자료를 기반으로 효과적인 프레젠테이션 슬라이드 구조를 작성합니다.`,
    outputFormat: `# 프레젠테이션 슬라이드

## 슬라이드 1: 제목
**[프레젠테이션 제목]**
부제목: [핵심 메시지]

📝 발표자 노트: [오프닝 멘트]

---

## 슬라이드 2: 목차/개요
- 섹션 1: [제목]
- 섹션 2: [제목]
- 섹션 3: [제목]

📝 발표자 노트: [전체 흐름 설명]

---

## 슬라이드 3: [핵심 내용 1]
• 포인트 1
• 포인트 2
• 포인트 3

📝 발표자 노트: [상세 설명]

---

[10-15개 슬라이드 계속...]

---

## 마지막 슬라이드: 요약 및 결론
**핵심 메시지:**
1. [요약 1]
2. [요약 2]
3. [요약 3]

📝 발표자 노트: [마무리 멘트와 Q&A 안내]`
  },
  'video-overview': {
    systemPrompt: `당신은 전문 콘텐츠 분석가입니다.
제공된 소스 자료를 제3자 관점에서 분석하고, 핵심 내용을 명확하게 설명합니다.
소스의 내용을 직접 읽는 게 아니라, 분석하고 해석해서 쉽게 풀어 설명합니다.
마크다운 기호는 사용하지 마세요. 순수 텍스트로만 작성하세요.`,
    outputFormat: `동영상 개요

[슬라이드 1] 오프닝
제목: 소스 자료의 핵심 주제
부제: 한 줄 요약

대본:
오늘 분석할 자료를 소개하겠습니다. 이 자료의 핵심은...

[슬라이드 2] 자료 소개
이 자료가 무엇인지 설명
작성 배경과 목적
왜 중요한지

대본:
먼저 이 자료가 무엇인지 설명드리겠습니다...

[슬라이드 3] 핵심 내용 1
첫 번째 주요 포인트
구체적인 내용
의미와 해석

대본:
첫 번째로 주목할 점은...

[슬라이드 4] 핵심 내용 2
두 번째 주요 포인트
구체적인 내용
의미와 해석

대본:
두 번째로 중요한 부분은...

[슬라이드 5] 핵심 내용 3
세 번째 주요 포인트
구체적인 내용
의미와 해석

대본:
세 번째로 살펴볼 내용은...

[슬라이드 6] 분석 및 시사점
전체 내용 분석
시사점과 의미
실무적 적용 방안

대본:
이 내용들을 종합해보면...

[슬라이드 7] 요약 및 정리
핵심 포인트 3-4개 정리
기억해야 할 내용
결론

대본:
정리하자면 첫째... 둘째... 이상입니다.`
  },
  'briefing-doc': {
    systemPrompt: `당신은 전문 비즈니스 분석가입니다. 제공된 자료를 기반으로 전문적인 브리핑 문서를 작성합니다.`,
    outputFormat: `# 브리핑 문서

## 📋 요약 (Executive Summary)
[2-3문장으로 핵심 내용 요약]

## 🔑 핵심 포인트
1. [주요 발견사항 1]
2. [주요 발견사항 2]
3. [주요 발견사항 3]

## 📊 상세 분석
[자료의 주요 내용을 구조화하여 설명]

## 💡 시사점 및 권장사항
- [권장사항 1]
- [권장사항 2]
- [권장사항 3]

## 📝 추가 참고사항
[관련 컨텍스트나 배경 정보]`
  },
  'audio-overview': {
    systemPrompt: `당신은 전문 오디오 콘텐츠 크리에이터입니다. 제공된 자료를 분석하여 청취자가 쉽게 이해할 수 있는 오디오 스크립트를 작성합니다.`,
    outputFormat: `# 오디오 개요

## 🎙️ 오프닝 (30초)
[친근한 인사와 주제 소개]

## 📖 본문 (3-5분)
### 섹션 1: [핵심 주제]
[상세 설명 - 청취자가 듣기만 해도 이해할 수 있도록]

### 섹션 2: [세부 내용]
[예시와 비유를 활용한 설명]

### 섹션 3: [추가 정보]
[관련 맥락과 배경]

## 🎯 핵심 요약 (30초)
- 포인트 1
- 포인트 2
- 포인트 3

## 👋 마무리 (15초)
[감사 인사와 마무리]`
  },
  'mindmap': {
    systemPrompt: `당신은 시각적 사고 전문가입니다. 제공된 자료를 분석하여 체계적인 마인드맵 구조를 생성합니다. 중심 주제에서 가지가 뻗어나가는 형태로 정보를 조직화합니다.`,
    outputFormat: `# 마인드맵

## 🎯 중심 주제
**[핵심 주제]**

## 🌿 주요 가지 1: [카테고리명]
- 하위 항목 1.1
  - 세부 사항
  - 세부 사항
- 하위 항목 1.2
  - 세부 사항

## 🌿 주요 가지 2: [카테고리명]
- 하위 항목 2.1
  - 세부 사항
- 하위 항목 2.2
  - 세부 사항

## 🌿 주요 가지 3: [카테고리명]
- 하위 항목 3.1
- 하위 항목 3.2

## 🌿 주요 가지 4: [카테고리명]
- 하위 항목 4.1
- 하위 항목 4.2

## 🔗 연결 관계
- [가지 1] ↔ [가지 2]: 관계 설명
- [가지 2] → [가지 3]: 영향 관계`
  },
  'report': {
    systemPrompt: `당신은 전문 비즈니스 리포트 작성자입니다. 제공된 자료를 분석하여 체계적이고 전문적인 보고서를 작성합니다.`,
    outputFormat: `# 분석 보고서

## 📋 개요
**작성 목적**: [보고서 목적]
**분석 대상**: [분석 자료 요약]

## 🔍 현황 분석
### 주요 발견사항
1. [발견사항 1]
2. [발견사항 2]
3. [발견사항 3]

### 데이터 분석
- 핵심 지표 1: [수치/내용]
- 핵심 지표 2: [수치/내용]
- 핵심 지표 3: [수치/내용]

## 💡 인사이트
### 강점
- [강점 1]
- [강점 2]

### 개선 필요 영역
- [개선점 1]
- [개선점 2]

## 📊 결론 및 권장사항
### 결론
[종합적인 결론 2-3문장]

### 권장사항
1. **단기 (1-3개월)**: [권장사항]
2. **중기 (3-6개월)**: [권장사항]
3. **장기 (6개월+)**: [권장사항]

## 📎 부록
[추가 참고 자료 및 출처]`
  },
  'flashcard': {
    systemPrompt: `당신은 교육 콘텐츠 전문가입니다. 제공된 자료에서 핵심 개념을 추출하여 학습용 플래시카드를 생성합니다. 각 카드는 질문-답변 형식으로 기억하기 쉽게 작성합니다.`,
    outputFormat: `# 플래시카드 세트

## 카드 1
**Q**: [질문]
**A**: [답변]
📝 힌트: [기억 팁]

---

## 카드 2
**Q**: [질문]
**A**: [답변]
📝 힌트: [기억 팁]

---

## 카드 3
**Q**: [질문]
**A**: [답변]
📝 힌트: [기억 팁]

---

[10-20개 카드 계속...]

---

## 복습 요약
**핵심 개념 정리**:
1. [개념 1]
2. [개념 2]
3. [개념 3]`
  },
  'quiz': {
    systemPrompt: `당신은 교육 평가 전문가입니다. 제공된 자료를 기반으로 이해도를 점검할 수 있는 퀴즈 문제를 생성합니다. 다양한 유형의 문제를 포함합니다.`,
    outputFormat: `# 퀴즈

## 📝 객관식 문제

### 문제 1
[질문]
- A) [선택지]
- B) [선택지]
- C) [선택지]
- D) [선택지]

**정답**: [정답]
**해설**: [해설]

---

### 문제 2
[질문]
- A) [선택지]
- B) [선택지]
- C) [선택지]
- D) [선택지]

**정답**: [정답]
**해설**: [해설]

---

## ✍️ 단답형 문제

### 문제 3
[질문]

**정답**: [정답]
**해설**: [해설]

---

## 🤔 서술형 문제

### 문제 4
[질문]

**모범 답안**: [답안]
**채점 기준**: [기준]

---

## 📊 정답 및 해설
| 문제 | 정답 | 난이도 |
|------|------|--------|
| 1 | [정답] | ⭐ |
| 2 | [정답] | ⭐⭐ |
| 3 | [정답] | ⭐⭐ |
| 4 | [정답] | ⭐⭐⭐ |`
  },
  'infographic': {
    systemPrompt: `당신은 인포그래픽 디자인 전문가입니다. 제공된 자료를 시각적으로 표현하기 좋은 인포그래픽 구조로 변환합니다. 숫자, 통계, 비교, 프로세스를 시각화합니다.`,
    outputFormat: `# 인포그래픽

## 📌 헤드라인
**[핵심 메시지 - 한 문장으로]**

## 📊 핵심 통계
| 항목 | 수치 | 의미 |
|------|------|------|
| [항목1] | [수치] | [설명] |
| [항목2] | [수치] | [설명] |
| [항목3] | [수치] | [설명] |

## 🔄 프로세스/단계
1️⃣ **[단계 1]**: [설명]
   ⬇️
2️⃣ **[단계 2]**: [설명]
   ⬇️
3️⃣ **[단계 3]**: [설명]
   ⬇️
4️⃣ **[단계 4]**: [설명]

## ⚖️ 비교 분석
| 기준 | 항목 A | 항목 B |
|------|--------|--------|
| [기준1] | [값] | [값] |
| [기준2] | [값] | [값] |
| [기준3] | [값] | [값] |

## 🎯 핵심 포인트
✅ [포인트 1]
✅ [포인트 2]
✅ [포인트 3]

## 💡 결론
[시각적으로 기억할 핵심 메시지]`
  },
  'data-table': {
    systemPrompt: `당신은 데이터 분석 전문가입니다. 제공된 자료에서 구조화된 데이터를 추출하여 체계적인 표 형식으로 정리합니다.`,
    outputFormat: `# 데이터 표

## 📋 데이터 개요
**데이터 출처**: [출처]
**분석 기준**: [기준]
**데이터 기간**: [기간]

## 📊 주요 데이터 표

### 표 1: [표 제목]
| 항목 | 값 1 | 값 2 | 값 3 | 비고 |
|------|------|------|------|------|
| [항목1] | [값] | [값] | [값] | [설명] |
| [항목2] | [값] | [값] | [값] | [설명] |
| [항목3] | [값] | [값] | [값] | [설명] |
| **합계/평균** | [값] | [값] | [값] | - |

### 표 2: [표 제목]
| 카테고리 | 세부 항목 | 수치 | 비율 |
|----------|----------|------|------|
| [카테고리1] | [항목] | [수치] | [%] |
| [카테고리2] | [항목] | [수치] | [%] |
| [카테고리3] | [항목] | [수치] | [%] |

## 📈 데이터 요약
- **최대값**: [항목] - [값]
- **최소값**: [항목] - [값]
- **평균**: [값]
- **특이사항**: [설명]

## 💡 데이터 인사이트
1. [인사이트 1]
2. [인사이트 2]
3. [인사이트 3]`
  }
}

export async function POST(req: Request) {
  try {
    const { type, prompt, sources, images } = await req.json() as {
      type: GenerationType
      prompt: string
      sources: Source[]
      images?: ImageSource[] // video-overview에서 전달되는 이미지들
    }

    if (!type || !sources || sources.length === 0) {
      return NextResponse.json({ error: '타입과 소스가 필요합니다' }, { status: 400 })
    }

    const generationConfig = GENERATION_PROMPTS[type]
    if (!generationConfig) {
      return NextResponse.json({ error: '지원하지 않는 생성 타입입니다' }, { status: 400 })
    }

    // Build context from sources
    const sourceContext = sources.map((s, i) => {
      const content = s.content || s.summary || ''
      return `[소스 ${i + 1}: ${s.title}]\n타입: ${s.type}\n내용:\n${content.slice(0, 15000)}`
    }).join('\n\n---\n\n')

    // Use Gemini for content generation
    const client = getGeminiClient()
    const model = client.getGenerativeModel({
      model: 'gemini-2.0-flash',
      generationConfig: {
        temperature: 0.7,
        maxOutputTokens: 8192
      }
    })

    // video-overview: 소스 분석 → 슬라이드 구조 생성 → 슬라이드별 대본 작성 → 크리에이터 TTS
    if (type === 'video-overview') {
      console.log(`[Generate] video-overview - 소스 기반 슬라이드 재구성 시작`)

      // ===== 1단계: 소스 자료 심층 분석 + 슬라이드 구조 생성 =====
      const slideStructurePrompt = `당신은 최고의 프레젠테이션 전략가입니다.
제공된 자료를 분석하여 유튜브 설명 영상에 적합한 슬라이드 구조를 설계하세요.

## 원본 자료
${sourceContext}

## 슬라이드 구조 설계 원칙
1. 자료의 핵심 메시지를 추출하여 논리적 흐름으로 재구성
2. 각 슬라이드는 하나의 핵심 포인트만 담음
3. 시청자가 쉽게 이해할 수 있는 순서로 배열
4. 8-12개 슬라이드 (오프닝, 본론 6-10개, 마무리)

## 출력 형식 (반드시 이 JSON 형식으로)
\`\`\`json
{
  "title": "영상 제목",
  "summary": "전체 요약 (1-2문장)",
  "slides": [
    {
      "slideNumber": 1,
      "slideTitle": "슬라이드 제목",
      "keyPoint": "이 슬라이드의 핵심 메시지 (1문장)",
      "details": ["세부 포인트 1", "세부 포인트 2", "세부 포인트 3"],
      "visualSuggestion": "이 슬라이드에 어울리는 시각자료 설명"
    }
  ]
}
\`\`\`

자료를 깊이 분석하고 슬라이드 구조를 생성하세요.`

      const slideStructureResult = await model.generateContent(slideStructurePrompt)
      const slideStructureText = await slideStructureResult.response.text()
      console.log(`[Generate] 슬라이드 구조 생성 완료`)

      // JSON 파싱
      let slideStructure: {
        title: string
        summary: string
        slides: Array<{
          slideNumber: number
          slideTitle: string
          keyPoint: string
          details: string[]
          visualSuggestion: string
        }>
      }

      try {
        // JSON 블록 추출
        const jsonMatch = slideStructureText.match(/```json\s*([\s\S]*?)\s*```/)
        const jsonText = jsonMatch ? jsonMatch[1] : slideStructureText
        slideStructure = JSON.parse(jsonText)
      } catch {
        console.error('[Generate] 슬라이드 구조 JSON 파싱 실패, 기본 구조 사용')
        slideStructure = {
          title: '자료 분석',
          summary: '소스 자료 기반 분석 영상',
          slides: [
            { slideNumber: 1, slideTitle: '오프닝', keyPoint: '오늘 주제 소개', details: [], visualSuggestion: '' },
            { slideNumber: 2, slideTitle: '핵심 내용', keyPoint: '주요 포인트', details: [], visualSuggestion: '' },
            { slideNumber: 3, slideTitle: '마무리', keyPoint: '요약 및 정리', details: [], visualSuggestion: '' }
          ]
        }
      }

      // ===== 2단계: 슬라이드별 크리에이터 대본 작성 =====
      const slidesForScript = slideStructure.slides.map(s =>
        `[슬라이드 ${s.slideNumber}: ${s.slideTitle}]
핵심: ${s.keyPoint}
세부: ${s.details.join(', ')}`
      ).join('\n\n')

      const creatorScriptPrompt = `당신은 100만 구독자 유튜브 크리에이터입니다.
아래 슬라이드 구조에 맞춰 실제로 촬영할 대본을 작성하세요.

## 슬라이드 구조
${slidesForScript}

## 원본 자료 (참고용)
${sourceContext.slice(0, 8000)}

## 대본 작성 규칙

### 말투 (매우 중요!)
- "~입니다/~합니다" 절대 금지!
- "~거든요", "~잖아요", "~죠", "~네요", "~예요" 사용
- "여러분", "자", "근데요", "진짜", "완전", "대박" 등 구어체
- 감탄사: "와", "우와", "헐", "오" 자연스럽게 사용

### 구조
1. 오프닝 (15초): 시청자 인사 + 오늘 영상 주제 + 왜 중요한지
2. 본론 (각 슬라이드당 20-40초): 슬라이드 내용을 자연스럽게 설명
3. 마무리 (15초): 핵심 요약 + 마무리 인사

### 예시 어투
"안녕하세요 여러분! 오늘 진짜 재밌는 거 가져왔거든요. 뭐냐면요..."
"자, 첫 번째로 얘기할 건요, [주제]인데요. 이게 왜 중요하냐면요..."
"근데 여기서 진짜 핵심은요, [포인트]거든요. 대박이죠?"
"쉽게 말하면요, [비유]랑 비슷한 거예요. 이해 되시죠?"
"정리하자면요! [요약]. 오늘 영상 여기까지고요, 유익했다면 좋아요 눌러주세요!"

### 금지 사항
- "이 슬라이드에서는...", "보시면...", "이 그림은..." 같은 슬라이드 언급 금지
- 딱딱한 문어체, 강연체 금지
- "~입니다", "~합니다" 금지

## 출력
슬라이드 번호 표시 없이, 자연스럽게 이어지는 하나의 대본으로 작성하세요.
총 2-3분 분량 (약 600-900자)의 크리에이터 스타일 대본.`

      const creatorScriptResult = await model.generateContent(creatorScriptPrompt)
      const creatorScript = await creatorScriptResult.response.text()
      console.log(`[Generate] 크리에이터 대본 생성 완료`)

      // ===== 3단계: TTS 생성 =====
      let audioUrl: string | undefined
      const ttsApiKey = process.env.GOOGLE_TTS_API_KEY || process.env.GOOGLE_API_KEY

      if (ttsApiKey && creatorScript.length > 0) {
        try {
          // 대본 텍스트 정규화
          const normalizeText = (text: string) => {
            let result = text
            // 마크다운 제거
            result = result.replace(/\*\*([^*]+)\*\*/g, '$1')
            result = result.replace(/\*([^*]+)\*/g, '$1')
            result = result.replace(/[#*_~`|]/g, '')
            result = result.replace(/\n+/g, ' ')
            // 약어 변환
            const abbreviations: Record<string, string> = {
              'AI': '에이아이', 'API': '에이피아이', 'LLM': '엘엘엠',
              'GPT': '지피티', 'CEO': '씨이오', 'CTO': '씨티오',
              'B2B': '비투비', 'B2C': '비투씨', 'MVP': '엠브이피',
              'SaaS': '사스', 'ESG': '이에스지', 'R&D': '알앤디',
              'KPI': '케이피아이', 'ROI': '알오아이', 'VC': '브이씨'
            }
            for (const [abbr, reading] of Object.entries(abbreviations)) {
              result = result.replace(new RegExp(`\\b${abbr}\\b`, 'g'), reading)
            }
            return result.trim()
          }

          const normalizedScript = normalizeText(creatorScript)

          // Google Cloud TTS API - Neural2 (가장 자연스러운 음성)
          const ttsResponse = await fetch(
            `https://texttospeech.googleapis.com/v1/text:synthesize?key=${ttsApiKey}`,
            {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({
                input: { text: normalizedScript.slice(0, 5000) },
                voice: {
                  languageCode: 'ko-KR',
                  name: 'ko-KR-Neural2-C' // Neural2 남성 (가장 자연스러움)
                },
                audioConfig: {
                  audioEncoding: 'MP3',
                  pitch: 0,
                  speakingRate: 1.05, // 약간 빠르게 (유튜브 느낌)
                  effectsProfileId: ['large-home-entertainment-class-device']
                }
              })
            }
          )

          if (ttsResponse.ok) {
            const ttsData = await ttsResponse.json()
            const audioData = ttsData.audioContent
            if (audioData) {
              audioUrl = `data:audio/mp3;base64,${audioData}`
              console.log(`[Generate] TTS 생성 완료`)
            }
          } else {
            const errorText = await ttsResponse.text()
            console.error('[Generate] TTS API error:', errorText)
          }
        } catch (err) {
          console.error('[Generate] TTS generation error:', err)
        }
      }

      // 최종 응답: 슬라이드 구조 + 대본 + 오디오
      const formattedContent = `# ${slideStructure.title}

## 영상 개요
${slideStructure.summary}

---

## 슬라이드 구조

${slideStructure.slides.map(s => `### 슬라이드 ${s.slideNumber}: ${s.slideTitle}
**핵심**: ${s.keyPoint}
${s.details.length > 0 ? s.details.map(d => `- ${d}`).join('\n') : ''}
`).join('\n')}

---

## 크리에이터 대본

${creatorScript}`

      return NextResponse.json({
        success: true,
        content: formattedContent,
        type,
        slideStructure, // 프론트엔드에서 슬라이드 구조 활용 가능
        script: creatorScript, // 순수 대본만
        audioUrl
      })
    }

    // 이미지가 없으면 기존 텍스트 기반 생성
    const fullPrompt = `${generationConfig.systemPrompt}

## 자료
${sourceContext}

## 요청
${prompt}

## 출력 형식
다음 형식에 맞춰 한국어로 작성해주세요:

${generationConfig.outputFormat}

위 형식을 참고하여 제공된 자료를 기반으로 내용을 작성해주세요. 자료에 없는 내용은 추측하지 말고, 자료에서 직접적으로 도출할 수 있는 내용만 포함해주세요.`

    const result = await model.generateContent(fullPrompt)
    const content = await result.response.text()

    return NextResponse.json({
      success: true,
      content,
      type
    })
  } catch (error) {
    console.error('Content generation error:', error)
    return NextResponse.json(
      { error: '콘텐츠 생성 중 오류가 발생했습니다' },
      { status: 500 }
    )
  }
}
